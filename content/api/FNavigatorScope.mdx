---
title: FNavigatorScope
description: "An inherited element that carries a FNavigatorHandle through the element tree. Descendants retrieve it with context.GetInherited<FNavigatorScope>().Navigator."
---

# FNavigatorScope

An inherited element that carries a FNavigatorHandle through the element tree. Descendants retrieve it with context.GetInherited<FNavigatorScope>().Navigator.

## Constructor

### FNavigatorScope

Creates an FNavigatorScope that propagates to the subtree rooted at .

| Parameter | Description |
| --- | --- |
| `navigator` | The navigator handle. Must not be null. |
| `child` | The child subtree. Must not be null. |
| `key` | An optional key for reconciliation identity. |

## Properties

### Navigator

The navigator handle available to descendants.

## Methods

### UpdateShouldNotify

