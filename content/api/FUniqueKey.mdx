---
title: FUniqueKey
description: "A key that guarantees uniqueness. No two UniqueKey instances are ever equal, even if created with the same parameters. Useful when an element must never be reused during reconciliation."
---

# FUniqueKey

A key that guarantees uniqueness. No two UniqueKey instances are ever equal, even if created with the same parameters. Useful when an element must never be reused during reconciliation.

