---
title: AnimationIntegrationTests
description: "Verifies that FAnimationController drives rebuilds through a real element tree when ticked via FAnimationSystem. Also validates the frozen-callback limitation as a documented contract."
---

# AnimationIntegrationTests

Verifies that FAnimationController drives rebuilds through a real element tree when ticked via FAnimationSystem. Also validates the frozen-callback limitation as a documented contract.

