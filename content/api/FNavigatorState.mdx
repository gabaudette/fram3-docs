---
title: FNavigatorState
description: "Holds the mutable navigation stack for an FNavigator. Exposes push and pop operations that trigger a rebuild of the navigator subtree. Obtain an instance via context.GetInherited<FNavigatorScope>().Navigator."
---

# FNavigatorState

Holds the mutable navigation stack for an FNavigator. Exposes push and pop operations that trigger a rebuild of the navigator subtree. Obtain an instance via context.GetInherited<FNavigatorScope>().Navigator.

## Properties

### CanPop

## Methods

### Build

### InitState

### Pop

### Push

