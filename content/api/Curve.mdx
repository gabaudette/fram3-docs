---
title: Curve
description: "live An easing function that maps a normalized linear progress value in [0, 1] to a curved output value. The input and output are both conventionally in [0, 1], though overshoot curves may return values outside that range."
status: "live"
---

# Curve

live An easing function that maps a normalized linear progress value in [0, 1] to a curved output value. The input and output are both conventionally in [0, 1], though overshoot curves may return values outside that range.
