---
title: IRootAttachedElement
description: "live Marker interface for elements that must be attached directly to the root container rather than to their logical parent in the visual tree. The renderer skips normal parent attachment and calls _rootContainer.Add for any element implementing this interface."

status: "live"

---

# IRootAttachedElement

live Marker interface for elements that must be attached directly to the root container rather than to their logical parent in the visual tree. The renderer skips normal parent attachment and calls _rootContainer.Add for any element implementing this interface.
