---
title: RebuildSchedulerStressTests
description: "Stress-tests the FRebuildScheduler inside a real tree. Validates depth-ordered processing, coalescing of duplicate dirty marks, and re-entrant scheduling (a rebuild that marks another node dirty within the same flush)."
---

# RebuildSchedulerStressTests

Stress-tests the FRebuildScheduler inside a real tree. Validates depth-ordered processing, coalescing of duplicate dirty marks, and re-entrant scheduling (a rebuild that marks another node dirty within the same flush).

