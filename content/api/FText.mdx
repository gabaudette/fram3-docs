---
title: FText
description: "Displays a string of text with an optional FTextStyle. This is a leaf element that maps directly to a UIToolkit Label."
---

# FText

Displays a string of text with an optional FTextStyle. This is a leaf element that maps directly to a UIToolkit Label.

## Constructor

### FText

Creates an FText element.

| Parameter | Description |
| --- | --- |
| `text` | The text content to display. |
| `style` | An optional text style. Null inherits from the parent. |
| `key` | An optional key for reconciliation identity. |

## Properties

### Style

The visual style applied to the text. Null inherits all values from the parent.

### Text

The text content to display.

