---
title: FRenderHandle
description: "Tracks the native VisualElement produced for a single FNode. The FRenderer maintains a dictionary of these handles keyed by node, updated on every mount, unmount, and rebuild. This is an internal framework type not intended for direct use."
---

# FRenderHandle

Tracks the native VisualElement produced for a single FNode. The FRenderer maintains a dictionary of these handles keyed by node, updated on every mount, unmount, and rebuild. This is an internal framework type not intended for direct use.

